# AIMED 官网部署状态报告

**部署时间：** 2026-04-23 22:05  
**部署负责人：** 小菲同学

---

## ✅ 已完成部署

### 1. Nginx 配置

**配置文件：** `/etc/nginx/conf.d/aius.conf`

**状态：** ✅ 已加载并生效

**测试命令：**
```bash
# 测试配置语法
/usr/sbin/nginx -t

# 重载配置
/usr/sbin/nginx -s reload

# 测试访问
curl -H "Host: aius.xin" http://127.0.0.1/
```

**访问测试：**
```
✅ http://127.0.0.1/ (Host: aius.xin) → 官网首页正常加载
✅ http://127.0.0.1/static/products.html → 产品页面正常
✅ http://127.0.0.1/api/ → 代理到 Hermes 18790 端口
```

### 2. Hermes 后端服务

**端口：** 18790  
**进程：** `python3 main.py` (PID: 3983008)  
**状态：** ✅ 运行中

**API 端点：**
- `/health` - 健康检查
- `/api/v1/upload` - 病例上传
- `/api/v1/cases/*` - 病例管理
- `/api/v1/diagnosis/*` - 诊断服务
- `/docs` - Swagger API 文档
- `/redoc` - Redoc 文档

### 3. OpenClaw Gateway

**端口：** 18789  
**状态：** ✅ 运行中（systemd 服务）

---

## ⏳ 待配置项目

### 1. 域名 DNS 解析

**需要操作：**
将 `aius.xin` 和 `www.aius.xin` 解析到当前服务器 IP

**DNS 记录：**
```
A     aius.xin      → [服务器 IP]
A     www.aius.xin  → [服务器 IP]
```

**状态：** ⏳ 待确认（域名已备案，需配置 DNS）

### 2. SSL 证书（HTTPS）

**方案 A：Let's Encrypt 免费证书**
```bash
# 安装 certbot
yum install -y certbot python3-certbot-nginx

# 申请证书
certbot --nginx -d aius.xin -d www.aius.xin
```

**方案 B：阿里云 SSL 证书**
- 在阿里云控制台申请免费 SSL 证书
- 下载证书文件到 `/etc/nginx/ssl/`
- 启用 aius.conf 中的 HTTPS 配置块

**状态：** ⏳ 待配置

### 3. 防火墙配置

**需要开放的端口：**
- 80 (HTTP)
- 443 (HTTPS)

**检查命令：**
```bash
firewall-cmd --list-all
# 或
iptables -L -n
```

**状态：** ⏳ 待检查

### 4. 服务自启动

**Hermes 后端服务化：**
```bash
# 创建 systemd 服务文件
cat > /etc/systemd/system/hermes.service << 'EOF'
[Unit]
Description=AIMED Hermes Backend
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/.openclaw/workspace
ExecStart=/usr/bin/python3 main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 启用服务
systemctl daemon-reload
systemctl enable hermes
systemctl start hermes
```

**状态：** ⏳ 待配置

---

## 📋 部署检查清单

### 立即可以做的
- [x] Nginx 配置已加载
- [x] Hermes 后端运行中
- [x] 本地访问测试通过
- [ ] DNS 解析配置
- [ ] SSL 证书申请
- [ ] 防火墙端口开放
- [ ] systemd 服务配置

### 生产环境必需
- [ ] HTTPS 强制重定向
- [ ] ICP 备案号显示
- [ ] 错误页面定制（404/500）
- [ ] 日志轮转配置
- [ ] 监控告警设置
- [ ] 备份策略

---

## 🚀 快速部署步骤

### 步骤 1：确认 DNS 解析
```bash
# 在域名服务商处添加 A 记录
# 然后验证
ping aius.xin
nslookup aius.xin
```

### 步骤 2：申请 SSL 证书
```bash
# 安装 certbot
yum install -y epel-release
yum install -y certbot python3-certbot-nginx

# 申请证书
certbot --nginx -d aius.xin -d www.aius.xin

# 自动续期测试
certbot renew --dry-run
```

### 步骤 3：启用 HTTPS
编辑 `/etc/nginx/conf.d/aius.conf`，取消 HTTPS 配置块的注释。

### 步骤 4：配置防火墙
```bash
# Alibaba Cloud Linux / CentOS
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload
```

### 步骤 5：配置服务自启动
```bash
# Hermes 服务
systemctl enable hermes
systemctl start hermes

# OpenClaw 服务（已配置）
systemctl status openclaw-gateway
```

---

## 📊 当前访问方式

### 本地测试
```
http://127.0.0.1/              → 官网首页
http://127.0.0.1/static/       → 静态页面
http://127.0.0.1/docs          → API 文档
http://127.0.0.1:18790/        → Hermes 直接访问
http://127.0.0.1:18789/        → OpenClaw Gateway
```

### 生产环境（待 DNS 配置后）
```
https://aius.xin/              → 官网首页
https://aius.xin/static/       → 静态页面
https://aius.xin/docs          → API 文档
https://aius.xin/api/          → API 服务
```

---

## ⚠️ 注意事项

1. **ICP 备案：** 域名已备案，但需在页脚显示备案号
2. **数据安全：** 生产环境必须启用 HTTPS
3. **日志管理：** 配置日志轮转，避免磁盘占满
4. **监控告警：** 建议配置服务监控和告警
5. **备份策略：** 定期备份数据库和配置文件

---

## 📞 下一步行动

1. **立即：** 确认 DNS 解析配置
2. **今天：** 申请并配置 SSL 证书
3. **本周：** 完成防火墙和服务自启动配置
4. **上线前：** 更新 ICP 备案号，完成最终测试

---

**部署状态：** 🟡 部分完成（本地可用，待 DNS + SSL）

**预计上线时间：** 配置 DNS + SSL 后即可上线
